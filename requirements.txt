# Direct dependencies, pinned to exact versions for reproducibility. The VM
# installs the fully-resolved requirements.lock.txt (all transitive deps)
# which deploy/startup.sh prefers when present; these are the direct-dep pins
# used for local development and the Phase 0 scratch-venv reproduction.
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-dotenv==1.2.2
PyYAML==6.0.3
httpx==0.28.1
pynacl==1.6.2
cryptography==49.0.0
# Verifies the Cloud IAP assertion in src/web_security.py. Required whenever
# IAP_AUDIENCE is set; harmless otherwise. Not installed in the local venv (its
# import is deferred and tests inject a verifier); pinned to the lock-file
# version resolved for the VM.
google-auth==2.56.2
pytest==9.1.1
