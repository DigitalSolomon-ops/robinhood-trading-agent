# Cloud Build config for the Options Trader runtime image (non-default Dockerfile name).
# Invoked by deploy/deploy-options-trader.sh with --substitutions=_IMAGE=...
steps:
  - name: gcr.io/cloud-builders/docker
    args: ["build", "-f", "Dockerfile.options-trader", "-t", "${_IMAGE}", "."]
images: ["${_IMAGE}"]
